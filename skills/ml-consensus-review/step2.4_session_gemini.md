# Step 2.4 ML Expert Verification & Final Consolidated Plan

> *本建議是基於 `step2.3_session_codex.md` 與 `step2.2_session_gemini.md` 對 `binance_tech_idx_model_trainer.py` 的修改建議產出的綜合驗證報告。*

## 1. 核心風險驗證 (Consolidated Validation)

### 1.1 標籤與回測正確性 (Label & Backtest Realism) - **Blocker**
- **驗證結果**：[Consensus Reached]。代碼中的 `create_multi_class_target` 與 `run_backtest_stats` 確實忽略了 K 線內部的價格路徑（Path-dependency）。
- **專家評估**：這是金融時序 ML 的致命傷，會導致模型學會「偷看未來路徑聚合結果」而非真實交易事件。
- **修正建議**：優先實現 **First-touch (Triple-Barrier)** 標籤與 **Event-driven** 回測。

### 1.2 門檻搜尋與執行規則脫鉤 (Decision Rule Mismatch) - **High Priority**
- **驗證結果**：[New Insight / Consensus Reached]。
- **專家評估**：`find_best_thresholds_on_val` 目前分開對 Short/Long 找最佳點，但 `final_blind_test` 卻是同時套用並處理衝突。這意味著「優化目標」與「最終行為」不一致。
- **修正建議**：重構 `find_best_thresholds_on_val`，使其針對 (Short_TH, Long_TH) 的二維組合進行聯合優化。

### 1.3 驗證衛生與再現性 (Validation Hygiene & Reproducibility) - **High Priority**
- **驗證結果**：[Consensus Reached]。
- **專家評估**：`gap` 的硬編碼與 `datetime.now()` 的使用會導致數據洩漏風險（Data Leakage）與實驗無法重現。
- **修正建議**：動態計算 `gap` 並固定訓練與測試的起訖時間。

---

## 2. 爭議項目裁決表 (Expert Ruling Table)

| 爭議項目 | Codex 觀點 | Gemini 觀點 | **專家裁決 (共識狀態)** |
| :--- | :--- | :--- | :--- |
| **Rolling vs Expanding** | 應明確定義研究假設 | 被視為風格差異 | **[Pending Discussion]** Rolling 與 Expanding Window 代表了對 Regime Adaptation 的不同假設，不能僅視為風格。應在文件中明確理由。 |
| **文件同步優先級** | 應提前執行 (Phase 2) | 放在最後 (Phase 3) | **[Consensus Reached]** 文件漂移 (Documentation Drift) 已嚴重到影響後續開發，裁決將其提前。 |
| **Threshold Search** | 點出聯合決策不一致風險 | 未特別強調此 Mismatch | **[Consensus Reached]** 接受 Codex 的補充，這對模型最終績效有實質影響。 |

---

## 3. 綜合執行清單 (Consolidated Next Actions)

我將任務按「技術正確性 > 驗證衛生 > 維護與增強」重新排序：

### Phase 1: Core Correctness (核心正確性)
1. **[ ] 重構 Labeling**：實現 `first_touch_labeling` (Triple-Barrier)。
2. **[ ] 升級 Backtest**：實現路徑依賴的 Event Replay，同步補齊 `Profit Factor` 與 `MDD`。
3. **[ ] 聯合門檻優化**：讓 Threshold Search 模擬最終的 Joint Decision Rule。

### Phase 2: Validation & Maintenance (驗證衛生與維護)
4. **[ ] 動態 Gap 與再現性**：`gap = max(lookahead, lags)`，並固定 `start_dt`/`end_dt` 保存 metadata。
5. **[ ] 校準量化驗證**：支援 `isotonic` / `sigmoid` 比較，輸出 `Brier Score` 與 `Reliability Curve`。
6. **[ ] 文件同步**：更新 `Financial ML step by step.md`，反映當前的 ATR-based 標籤與 5-way split 流程。

### Phase 3: Research Enhancement (研究強化)
7. **[ ] 樣本權重 (Uniqueness)**：處理 Label Overlap 問題。
8. **[ ] 決策排序 (Score-based)**：探索 `p(long) - p(short)` 等連續信號排序。
9. **[ ] 其他優化**：Feature Importance 穩定性檢查、HPO 等。

---
*Generated by Gemini CLI - ML Expert Agent (Final Consolidated)*
